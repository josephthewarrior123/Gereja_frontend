export default class Constants {
    static ATTENDANCE_STATE = {
        APPROVE: 'approve',
        REJECT: 'reject',
    };
    
    static INVITATION_STATUS = {
        INVITED: 'INVITED',
        ACCEPTED: 'ACCEPTED',
        REJECTED: 'REJECTED',
        CHECKED_IN: 'CHECKED_IN',
    };

    static NAVIGATION_DRAWER_WIDTH = 240;
    static HEADER_MOBILE_HEIGHT = 72;
    static HEADER_DESKTOP_HEIGHT = 80;
}
